- name: AyCorp
  columns:
    - size: small
      widgets:
        $include: ../widgets/hardware.yml
        
    - size: full
      widgets:
        $include: ../widgets/monitors.yml

    - size: small
      widgets:
        $include: ../widgets/myspeed.yml
